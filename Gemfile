source "https://rubygems.org"

# Local preview only — GitHub Pages does not read this file. It builds the site
# on its own servers with Jekyll 3.9.x (the `github-pages` gem set).
#
# We deliberately do NOT use the `github-pages` gem here: it pins Liquid 4.0.3,
# which calls `tainted?` — removed from Ruby in 3.2 — so it cannot run on any
# modern Ruby. Jekyll 4 renders this site identically; see README.
gem "jekyll", "~> 4.4"
